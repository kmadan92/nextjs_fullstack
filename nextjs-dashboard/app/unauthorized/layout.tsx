

export const metadata = {
    title: 'Unauthorized'
}

export default function UnauthorizedLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}